import 'package:flutter/material.dart';

class B01PageUi extends StatelessWidget {
  const B01PageUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('B01 Page')),
      body: Center(
        child: Text('B01 Page'),
      ),
    );
  }
}