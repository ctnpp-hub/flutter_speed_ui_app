import 'package:flutter/material.dart';

class A02PageUi extends StatelessWidget {
  const A02PageUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('A02 Page')),
      body: Center(
        child: Text('A02 Page'),
      ),
    );
  }
}