import 'package:flutter/material.dart';

class A01PageUi extends StatelessWidget {
  const A01PageUi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('A01 Page')),
      body: Center(
        child: Text('A01 Page'),
      ),
    );
  }
}